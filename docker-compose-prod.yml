version: "3.8"
services:
  mongo:
    image: mongo
#    ports:
#      - 27017:27017
    networks:
      - db
    volumes:
      - mongodb:/data/db

  main-server:
    image: maciekek/my-pi-home-server
    restart: always
    ports:
      - "8888:8888"
    depends_on:
      - mongo
    networks:
      - db
      - app

  client:
    image: maciekek/my-pi-home-client
    restart: always
    ports:
      - "3000:3000"
    networks:
      - app
    environment:
      - REACT_APP_API_URL=http://main-server:8888

volumes:
   mongodb:

networks:
  db:
  app:
